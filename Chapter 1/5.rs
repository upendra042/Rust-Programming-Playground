/*5. Write a program to implement
a. Implicit type declaration
b. Explicit type declaration*/
fn main() {
    // Implicit type declaration
    let x = 42;
    println!("Implicitly typed x: {}", x);

    // Explicit type declaration
    let y: f64 = 3.14;
    println!("Explicitly typed y: {}", y);
}
